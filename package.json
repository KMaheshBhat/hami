{
  "name": "hami-frameworx-monorepo",
  "version": "0.1.0",
  "description": "Monorepo for HAMI FRAMEWORX",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "cli:dev": "bun run --cwd apps/hami-cli dev",
    "cli:install": "bun run --cwd apps/hami-cli build && cd apps/hami-cli && npm install -g .",
    "server:dev": "bun run --cwd apps/hami-server dev",
    "all:build": "bun run --filter=\"@hami-frameworx/core\" build && bun run --filter=\"@hami-frameworx/core-*\" build && bun run --filter=\"@hami-frameworx/hami-*\" build",
    "all:clean": "rm -rf apps/*/dist packages/*/dist",
    "node_modules:clean": "rm -rf apps/*/node_modules packages/*/node_modules"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}